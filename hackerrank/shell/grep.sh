#grep -w <the> -> case sensitive and matches whole words only

#grep -wi <the> -> match ignore case sensitive

#grep -wiv <that> -> Invert match 

#grep -Ei 'the|then|that|those' Match pattern case insensitive